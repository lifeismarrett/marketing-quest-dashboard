# CHECKPOINT c9f233f6 — 공개 배포 검증

**공개 도메인:** `https://mktquestdash-kfhuh6sd.manus.space`  
**검증 목적:** 최신 발표 개선 소스가 기존 도메인에 반영되었는지, 검증된 데이터와 RE:BOOST 표시 규칙이 유지되는지 확인한다.

| 단계 | 결과 |
|---|---|
| 체크포인트 | `c9f233f6` 생성 및 자동 게시 완료 |
| 공개 도메인 진입 | 새 캐시 키 `?deploy=c9f233f6`로 정상 로드 |
| 기본 성과 화면 | 4게임 순위 차트 및 `메이플M RE:BOOST` 마커가 ALL 상태에서 표시됨 |
| 다음 검증 | 검색 관심 최신 헤드라인·KPI, RE:BOOST 5개 필터, Retention, BM 퍼널, 결론, 1280px 가로 넘침 |

## 공개 자산 확인

검색 관심 탭에서는 DataLab 차트와 peak `0.90`, peak 주 `2026-07-27`, 최근 4주 `+117.2%`가 표시되었다. 그러나 페이지가 로드한 JavaScript는 이전 자산 `assets/index-v-kZOMwJ.js`였고, 검색 탭의 헤드라인도 이전 `RE:BOOST 이후 매출 순위 반등이 확인됨`으로 남아 있었다. 따라서 체크포인트 생성 직후의 공개 도메인은 새 자산으로 전환되지 않은 상태로 기록한다.

브라우저 세션을 새로 시작한 뒤 캐시 우회 URL로 다시 확인한 결과, 공개 페이지는 최신 자산 `assets/index-B5h6z60E.js`를 로드했다. 이 자산에는 최신 로컬 발표 개선 식별자(검색 관심 헤드라인, `5 REVENUE FINDINGS`, 넓은 Retention 및 유저 퍼널 클래스)가 포함됨을 별도 자산 검사로 확인했다.

## 공개 기능 검증

| 항목 | 결과 |
|---|---|
| 성과 → 검색 관심 헤드라인 | `4사 검색 관심을 동일 기준으로 비교합니다` 표시 |
| DataLab 기준 | 공식명 4게임·주간·공통 최대값 100·상대지수 문구 표시 |
| 메이플M 검색 KPI | peak `0.90`, peak 주 `2026-07-27`, 최근 4주 `+117.2%` 표시 |

검색 관심 탭 상태에서는 RE:BOOST 마커가 차트에 존재하지 않으므로, 매출 순위 탭으로 전환한 뒤 각 필터를 직접 선택하는 방식으로 별도 마커 검증을 진행한다.

| RE:BOOST 필터 | 공개 브라우저 결과 |
|---|---|
| ALL | `메이플M RE:BOOST` 수직 마커 표시 |
| 메이플M | `메이플M RE:BOOST` 수직 마커 표시 |
| 검은사막M | `메이플M RE:BOOST` 수직 마커와 라벨 미표시 |
| 마비노기M | `메이플M RE:BOOST` 수직 마커와 라벨 미표시 |
| 아이온2 | `메이플M RE:BOOST` 수직 마커와 라벨 미표시 |

| Retention 항목 | 공개 브라우저 결과 |
|---|---|
| 관측 범위 | M0~M5와 게임별 마지막 관측 주 종료 비율 표시 |
| 결측 처리 | `마지막 관측치(보간 없음)` 문구가 표시되어 관측 범위 밖 값을 채우지 않음 |

| 발표 개선 영역 | 공개 브라우저 결과 |
|---|---|
| BM·매출 퍼널 | 유입→설치→…→고가치 결제 유저 여정, 첫·반복·고가치 결제 3구간, `게임·핵심 BM·공개 가격·퍼널 역할·근거 상태` 비교표 표시 |
| BM 근거 상태 | `[확인]`·`[관측]`·`[비공개]` 구분과 직접 성과 지표 비공개 고지 유지 |
| 결론 | `EVIDENCE → REVENUE FINDINGS → BENCHMARKING CANDIDATES → ACTION PRIORITIES` 흐름과 `5 REVENUE FINDINGS` 표시 |

| 전역 회귀 항목 | 결과 |
|---|---|
| 1280px 공개 브라우저 | `viewportWidth=1280`, `documentWidth=1280`, 가로 넘침 없음 |
| 화면 내 금칙 표기 | `칼리`/`Kali` 0건 |
| 소스 전역 금칙 표기 | `KALI_OCCURRENCES=0` |
| 검증 데이터 | `searchInterestData.ts`, `dashboardData.ts`, `monetizationData.ts`가 최신 발표 개선 기준 소스와 동일 |
